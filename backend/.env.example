# Exemplo de variáveis de ambiente - Backend

# URL de conexão com PostgreSQL (ajustar para ambiente de produção)
DATABASE_URL=postgresql://usuario:senha@host:porta/nome_do_banco?schema=public

# Chave secreta para JWT (ALTERAR EM PRODUÇÃO - usar valor forte)
JWT_SECRET=sua_chave_secreta_forte_aqui

# URL pública da API (ajustar para domínio de produção)
PUBLIC_URL=https://api.seu-dominio.com

# Porta do servidor (opcional, padrão é 4000)
PORT=4000