# Arquivo de exemplo de ambiente - Backend

# URL de conexão com PostgreSQL (ajustar para ambiente específico)
DATABASE_URL=postgresql://usuario:senha@host:porta/nome_do_banco?schema=public

# Chave secreta para JWT (ALTERAR EM PRODUÇÃO - usar valor forte)
JWT_SECRET=sua_chave_secreta_forte_aqui

# URL pública da API (ajustar para domínio de produção com subdomínio)
PUBLIC_URL=https://api.nexusvalvulas.com.br

# Porta do servidor (opcional, padrão é 4000)
PORT=4000